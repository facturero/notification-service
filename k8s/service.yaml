apiVersion: v1
kind: Service
metadata:
  name: notification-service-node
  labels:
    app: notification-service-node
spec:
  type: NodePort
  selector:
    app: notification-service-node
  ports:
    - protocol: TCP
      port: 3011
      targetPort: 3011
